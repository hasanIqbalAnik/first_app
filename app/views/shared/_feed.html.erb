<% unless @feed.empty?%>
    <table class = "microposts" summary = "User Microposts">
      <%=	 render :partial => 'shared/feed', :collection => @feed_items %>

    </table>
    <%=	 will_paginate @feed_items %>


<% end %>